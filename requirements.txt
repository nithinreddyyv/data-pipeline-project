stage('Run in Docker') {
    steps {
        sh '''
        docker run --rm -v $(pwd):/app -w /app python:3.11 \
        sh -c "
        ls -la /app &&
        pip install -r /app/requirements.txt &&
        python /app/main.py &&
        python -m pytest
        "
        '''
    }
}